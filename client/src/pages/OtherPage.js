import React from 'react';
import { Container, Typography } from '@mui/material';

const OtherPage = () => {
    return (
        <Container>
            <Typography variant="h3" gutterBottom>
                Other
            </Typography>
            <Typography variant="body1">
                This is a placeholder for future functionalities.
            </Typography>
        </Container>
    );
};

export default OtherPage; // Asegúrate de exportar el componente como default